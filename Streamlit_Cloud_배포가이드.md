# 🚀 Streamlit Cloud 정식 배포 가이드

## 📋 준비물 체크리스트

- [x] 완성된 Streamlit 앱
- [ ] GitHub 계정
- [ ] Git 설치 (https://git-scm.com/downloads)
- [ ] 배포용 파일들 (아래 제공)

---

## 🎯 전체 과정 (약 10-15분)

```
1단계: GitHub 저장소 생성 (2분)
2단계: 프로젝트 파일 준비 (3분)
3단계: GitHub에 코드 업로드 (3분)
4단계: Streamlit Cloud 배포 (2분)
5단계: 확인 및 공유 (1분)
```

---

## 1️⃣ GitHub 저장소 생성

### 방법 A: GitHub 웹사이트에서 (쉬움)

1. https://github.com 접속
2. 로그인 (없으면 회원가입)
3. 우측 상단 `+` → `New repository` 클릭
4. 설정:
   ```
   Repository name: prepisco-finance-system
   Description: Prepisco Korea 재무관리 시스템
   Public ✅ (또는 Private - 무료로 배포 가능)
   Add README: ❌ (우리가 만듭니다)
   ```
5. `Create repository` 클릭

### 저장소 URL 복사 (중요!)

생성 후 나오는 URL을 복사해두세요:
```
https://github.com/USERNAME/prepisco-finance-system.git
```

---

## 2️⃣ 프로젝트 파일 준비

### 현재 프로젝트 폴더 구조 확인

```
프로젝트폴더/
├── main.py
├── file_engine.py
├── report_generator.py
├── excel_report.py
├── pages/
│   ├── 01_Finance.py
│   ├── 02_Contracts.py
│   ├── 04_HR.py
│   └── 05_PMS.py
├── workspaces/           ← 실제 데이터 (제외!)
├── closed_reports/       ← 실제 데이터 (제외!)
└── NotoSansKR-VF.ttf
```

### 필수 파일 추가 (아래 파일들 다운로드해서 프로젝트 폴더에 복사)

1. **requirements.txt** - 라이브러리 목록
2. **.gitignore** - 실제 데이터 제외
3. **README.md** - 프로젝트 설명
4. **create_sample_data.py** - 샘플 데이터 생성

---

## 3️⃣ GitHub에 코드 업로드

### Windows 사용자

**명령 프롬프트 또는 PowerShell 열기:**
```
Win + R → cmd 입력 → Enter
```

**프로젝트 폴더로 이동:**
```cmd
cd C:\Users\마채현\프로젝트폴더
```

**Git 초기화 및 업로드:**
```bash
# Git 초기화
git init

# 모든 파일 추가 (.gitignore가 실제 데이터 자동 제외)
git add .

# 첫 커밋
git commit -m "Initial commit: 재무관리 시스템 v1.0"

# GitHub 저장소 연결 (1단계에서 복사한 URL 사용)
git remote add origin https://github.com/USERNAME/prepisco-finance-system.git

# 업로드
git branch -M main
git push -u origin main
```

### Mac/Linux 사용자

**터미널 열기:**
```
Cmd + Space → Terminal 입력
```

**나머지는 Windows와 동일**

### ⚠️ 문제 해결

**에러: Git이 없다고 나오면?**
```
https://git-scm.com/downloads
에서 Git 다운로드 후 재시도
```

**에러: 로그인 요청이 나오면?**
```
GitHub 사용자명과 비밀번호 입력
(비밀번호 대신 Personal Access Token 필요할 수 있음)
```

**Personal Access Token 생성:**
1. GitHub → Settings → Developer settings
2. Personal access tokens → Tokens (classic)
3. Generate new token → repo 체크
4. 생성된 토큰을 비밀번호로 사용

---

## 4️⃣ Streamlit Cloud 배포

### 단계별 진행

1. **Streamlit Cloud 접속**
   ```
   https://share.streamlit.io
   ```

2. **GitHub 로그인**
   - `Continue with GitHub` 클릭
   - 권한 승인

3. **New app 클릭**

4. **설정 입력**
   ```
   Repository: USERNAME/prepisco-finance-system
   Branch: main
   Main file path: main.py
   
   App URL (선택):
   prepisco-finance (원하는 이름)
   ```

5. **Advanced settings (선택사항)**
   ```
   Python version: 3.9 (또는 3.10)
   ```

6. **Deploy! 클릭**

### 배포 진행 (2-3분 소요)

화면에 로그가 표시되며 다음 과정을 거칩니다:
```
📦 Installing dependencies...
🔧 Setting up environment...
🚀 Starting app...
✅ App is live!
```

---

## 5️⃣ 완성! 🎉

### 배포 완료 후

**생성된 URL:**
```
https://USERNAME-prepisco-finance-system-main-abc123.streamlit.app
```

또는 커스텀 URL 설정했다면:
```
https://prepisco-finance.streamlit.app
```

### 초기 데이터 세팅

배포된 앱에는 샘플 데이터가 없으므로:

**옵션 1: 수동 업로드**
- 앱 접속 → 파일 업로드 탭 → 샘플 엑셀 업로드

**옵션 2: 샘플 데이터 포함 (권장)**
```bash
# 로컬에서 샘플 데이터 생성
python create_sample_data.py

# Git에 추가 (.gitignore 수정 필요)
git add workspaces/
git commit -m "Add sample data"
git push
```

**⚠️ 주의:** 실제 업무 데이터는 절대 업로드하지 마세요!

---

## 🔗 대표님께 공유하기

### 공유 메시지 예시 (슬랙/카톡)

```
안녕하세요 대표님,

재무관리 시스템 개발이 완료되어 보고드립니다.

🔗 시스템 링크
https://prepisco-finance.streamlit.app

📊 주요 기능
• 월간 결산 자동화
• 매출/지출 자동 분류
• 데이터 시각화 대시보드
• 엑셀/PDF 보고서 생성
• 계약 및 프로젝트 관리

💻 접속 방법
PC, 모바일 모두 접속 가능합니다.
24시간 언제든지 확인하실 수 있습니다.

📝 사용 방법
1. 링크 클릭
2. 좌측 메뉴에서 원하는 기능 선택
3. 데이터는 샘플로 구성되어 있습니다

감사합니다.
```

---

## 🔧 배포 후 관리

### 코드 수정 및 재배포

**로컬에서 수정 후:**
```bash
git add .
git commit -m "수정 내용 설명"
git push
```

→ **자동으로 Streamlit Cloud에 반영됩니다!** (2-3분 소요)

### 배포 상태 확인

```
https://share.streamlit.io
→ 내 앱 목록에서 확인
→ 로그 확인 가능
```

### 앱 중지/삭제

**일시 중지:**
- Streamlit Cloud → 앱 선택 → Settings → Pause app

**완전 삭제:**
- Streamlit Cloud → 앱 선택 → Settings → Delete app

---

## 🔐 보안 설정 (선택)

### 비공개 저장소로 변경

**GitHub에서:**
```
Settings → General → Change visibility → Private
```

→ Streamlit Cloud에서도 그대로 작동!

### 비밀번호 보호 추가

**main.py 상단에 추가:**
```python
import streamlit as st

# 간단한 인증
if 'authenticated' not in st.session_state:
    st.session_state.authenticated = False

if not st.session_state.authenticated:
    with st.form("login_form"):
        st.title("🔒 로그인")
        password = st.text_input("비밀번호", type="password")
        submit = st.form_submit_button("로그인")
        
        if submit:
            if password == "prepisco2026":  # 원하는 비밀번호
                st.session_state.authenticated = True
                st.rerun()
            else:
                st.error("비밀번호가 틀렸습니다.")
    st.stop()

# 여기부터 기존 코드
st.title("💰 자금 관리 시스템")
# ...
```

---

## ❓ 자주 묻는 질문

### Q1: 비용이 드나요?
**A:** 완전 무료입니다! Streamlit Community Cloud는 무료로 사용 가능합니다.

### Q2: 몇 명이 동시 접속 가능한가요?
**A:** 무료 플랜에서 수십 명 동시 접속 가능합니다.

### Q3: 실제 업무 데이터를 사용해도 되나요?
**A:** 가능하지만 권장하지 않습니다. 보안이 중요하다면 사내 서버 사용을 권장합니다.

### Q4: 도메인을 바꿀 수 있나요?
**A:** 기본 URL은 `*.streamlit.app` 형태이며, 커스텀 도메인은 유료 플랜에서만 가능합니다.

### Q5: 배포 실패 시 어떻게 하나요?
**A:** Streamlit Cloud의 로그를 확인하세요. 대부분 requirements.txt 문제입니다.

---

## 🎯 다음 단계

배포가 완료되면:

1. ✅ 링크 테스트 (PC, 모바일)
2. ✅ 각 기능 정상 작동 확인
3. ✅ 대표님께 링크 공유
4. ✅ 피드백 받고 개선

---

## 💡 필요한 도움

막히는 부분이 있으면 언제든 물어보세요!

- Git 설정
- GitHub 업로드
- Streamlit Cloud 설정
- 에러 해결

모두 도와드릴게요! 🚀
