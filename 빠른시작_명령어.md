# ⚡ 빠른 시작 - 명령어 모음

## 📋 사전 준비

1. GitHub 계정 생성: https://github.com
2. Git 설치: https://git-scm.com/downloads
3. 다운로드 받은 파일들을 프로젝트 폴더에 복사:
   - requirements.txt
   - gitignore.txt → .gitignore 로 이름 변경
   - README.md
   - create_sample_data.py

---

## 🚀 실행 명령어 (복사해서 사용)

### 1단계: 프로젝트 폴더로 이동

**Windows:**
```cmd
cd C:\Users\마채현\프로젝트폴더
```

**Mac/Linux:**
```bash
cd ~/프로젝트폴더
```

---

### 2단계: Git 초기화 및 GitHub 업로드

```bash
# Git 초기화
git init

# 모든 파일 추가
git add .

# 커밋
git commit -m "Initial commit: 재무관리 시스템 v1.0"

# GitHub 저장소 연결 (아래 URL을 본인 것으로 변경!)
git remote add origin https://github.com/USERNAME/prepisco-finance-system.git

# 브랜치 이름 설정
git branch -M main

# 업로드
git push -u origin main
```

**⚠️ 주의:** `USERNAME`을 본인의 GitHub 사용자명으로 바꾸세요!

---

### 3단계: Streamlit Cloud 배포

1. https://share.streamlit.io 접속
2. `Continue with GitHub` 클릭
3. `New app` 클릭
4. 설정 입력:
   ```
   Repository: USERNAME/prepisco-finance-system
   Branch: main
   Main file path: main.py
   ```
5. `Deploy!` 클릭

---

## 🔄 코드 수정 후 재배포

```bash
# 수정 사항 추가
git add .

# 커밋 (메시지 수정)
git commit -m "기능 개선: 기타비용 상세 추가"

# 푸시
git push
```

→ 자동으로 Streamlit Cloud에 반영됩니다! (2-3분)

---

## 🎯 체크리스트

배포 전 확인:
- [ ] GitHub 저장소 생성 완료
- [ ] requirements.txt 프로젝트 폴더에 복사
- [ ] .gitignore 프로젝트 폴더에 복사 (이름 확인!)
- [ ] README.md 프로젝트 폴더에 복사
- [ ] Git 명령어 실행 완료
- [ ] GitHub에 코드 업로드 완료
- [ ] Streamlit Cloud 배포 완료

---

## 💡 꿀팁

### GitHub 저장소 빠르게 만들기

GitHub CLI 사용 (선택사항):
```bash
# GitHub CLI 설치 후
gh repo create prepisco-finance-system --public
```

### 로컬에서 테스트

배포 전 로컬에서 확인:
```bash
streamlit run main.py
```

→ http://localhost:8501 에서 확인

---

## ❌ 에러 해결

### "git: command not found"
```bash
# Git 설치 필요
https://git-scm.com/downloads
```

### "Permission denied (publickey)"
```bash
# HTTPS 방식으로 변경
git remote set-url origin https://github.com/USERNAME/prepisco-finance-system.git
```

### "failed to push some refs"
```bash
# 강제 푸시 (주의: 처음 한 번만)
git push -f origin main
```

### Streamlit Cloud 배포 실패
```
로그 확인 → 대부분 requirements.txt 문제
→ 파이썬 버전 확인 (3.9 또는 3.10 권장)
```

---

## 📞 도움이 필요하면

막히는 부분을 정확히 알려주세요:
1. 어느 단계에서 막혔는지
2. 에러 메시지 전체
3. 실행한 명령어

바로 해결해드리겠습니다! 🚀
